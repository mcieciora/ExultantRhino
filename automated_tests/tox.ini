[tox]
skipsdist = True
envlist = py310

[common]
deps =  pylint
        -r../requirements.txt

[testenv]
commands =
        python3 -V

[testenv:lint]
deps =  pylint
commands =  pylint {posargs:py.test} --max-line-length=120 --disable=C0114 --fail-under=8.0
